<header>
  <div>
    <% if current_user %>
      <%= current_user.username %>
      <%= link_to '登出', destroy_user_session_path, method: :delete, data: { alert: "Bye" } %>
    <% end %>
  </div>
  <div class="tabs">
    <a class="tab tab-lg tab-lifted"><%= category.first %></a> 
    <a class="tab tab-lg tab-lifted"><%= category.second %></a> 
    <a class="tab tab-lg tab-lifted"><%= category.third %></a>
    <a class="tab tab-lg tab-lifted"><%= category.last %></a>
  </div>
</header>